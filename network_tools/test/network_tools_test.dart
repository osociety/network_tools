const port = 53; //ssh (22) is for github actions and dns (53) for local machine
const timeout = Duration(milliseconds: 3000);
const lastHostId =
    1024; //keep it 128 for local tests, and 1024 for github actions
void main() {}
